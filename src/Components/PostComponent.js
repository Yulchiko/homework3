export default function PostComponent({post}) {
    return (
        <div>
            {post.title}
            {/*<button onClick={() => navigation.navigate[id.toString()]}>Click</button>/*/}
        </div>
    );
}